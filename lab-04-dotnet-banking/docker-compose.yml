version: '3.8'

services:
  banking-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - banking-data:/app/data
    restart: unless-stopped

volumes:
  banking-data:
