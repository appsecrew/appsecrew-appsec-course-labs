version: '3.8'

services:
  ecommerce-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///data/ecommerce.db
    restart: unless-stopped
